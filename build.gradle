apply plugin: 'java-library'
group = 'com.aevi.sdk.build'
version = '1.0.6'

apply from: 'src/main/resources/android/publishing-utils.gradle'

publishingUtils?.publication {
    groupId group
    artifactId project.name
    version version
}
